/**
 * `crash-on-dropped-frame` checks stay off for this many frames / paces after
 * session start (scene build, first uploads, shader compile, etc.).
 */
export const WEBXR_CRASH_ON_DROP_WARMUP_FRAMES = 100;
